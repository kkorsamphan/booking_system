import React from 'react';

import Box from '@mui/material/Box';

const MakeBookingPage = (props) => {
	return <Box sx={{ color: '#FFFFFF' }}>Make My Booking</Box>;
};

export default MakeBookingPage;
